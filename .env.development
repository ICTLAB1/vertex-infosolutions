# Loaded automatically by `next dev`, and by nothing else. Committed on purpose:
# these are not secrets, they are the settings that keep a developer's machine
# out of the live shop's numbers.

# No analytics from a laptop. Without this every local page load, every test run
# and every screenshot lands in the same GA4 property as real customers.
GTM_DISABLED=1
